<Window
    x:Class="Glance.Shell.WinUI.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Shell="using:Glance.Shell"
    xmlns:WinUI="using:Elysium.UI.WinUI">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid Loaded="HandleLoaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar
            x:Name="AppTitleBar"
            x:Uid="SettingsTitleBar"
            BackRequested="HandleBackRequested"
            IsBackButtonEnabled="False"
            IsBackButtonVisible="False" />
        <NavigationView
            Grid.Row="1"
            Margin="0,8,0,0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            MenuItemsSource="{x:Bind ViewModel, Mode=OneWay}"
            PaneDisplayMode="Left"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
            SelectionChanged="HandleNavigationSelectionChanged">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            </NavigationView.Resources>
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="Shell:ISettingViewModel">
                    <NavigationViewItem Margin="8,0,0,0">
                        <ContentControl
                            HorizontalContentAlignment="Stretch"
                            Content="{x:Bind}"
                            ContentTemplateSelector="{WinUI:ViewTemplateSelector}" />
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <BreadcrumbBar
                    Grid.Row="0"
                    Margin="36,0,36,16"
                    ItemClicked="HandleBreadcrumbItemClicked"
                    ItemsSource="{x:Bind BreadcrumbItems}">
                    <BreadcrumbBar.Resources>
                        <ResourceDictionary>
                            <x:Double x:Key="BreadcrumbBarItemThemeFontSize">20</x:Double>
                            <Thickness x:Key="BreadcrumbBarChevronPadding">8,4,8,0</Thickness>
                            <FontWeight x:Key="BreadcrumbBarItemFontWeight">SemiBold</FontWeight>
                            <x:Double x:Key="BreadcrumbBarChevronFontSize">16</x:Double>
                        </ResourceDictionary>
                    </BreadcrumbBar.Resources>
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontFamily="Segoe UI Variable Display" Text="{Binding}" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
                <Grid
                    Grid.Row="1"
                    Margin="36,0,36,12"
                    ColumnSpacing="12"
                    MinHeight="32"
                    Visibility="{x:Bind ToVisibility(ViewModel.CanReorderCurrentView), Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{x:Bind ViewModel.CurrentView.Description, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button
                            x:Uid="ModulesReorderCancelButton"
                            Width="32"
                            Height="32"
                            Click="HandleCancelReordering"
                            Padding="0"
                            Visibility="{x:Bind ToVisibility(ViewModel.IsReorderingCurrentView), Mode=OneWay}">
                            <FontIcon
                                FontFamily="Segoe Fluent Icons"
                                FontSize="12"
                                Glyph="&#xE711;" />
                        </Button>
                        <Button
                            x:Uid="ModulesReorderDoneButton"
                            Width="32"
                            Height="32"
                            Click="HandleCompleteReordering"
                            Padding="0"
                            Visibility="{x:Bind ToVisibility(ViewModel.IsReorderingCurrentView), Mode=OneWay}">
                            <FontIcon
                                FontFamily="Segoe Fluent Icons"
                                FontSize="12"
                                Glyph="&#xE73E;" />
                        </Button>
                    </StackPanel>
                </Grid>
                <ContentControl
                    Grid.Row="2"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Content="{x:Bind ViewModel.CurrentView, Mode=OneWay}">
                    <ContentControl.ContentTransitions>
                        <TransitionCollection>
                            <ContentThemeTransition />
                        </TransitionCollection>
                    </ContentControl.ContentTransitions>
                    <ContentControl.ContentTemplate>
                        <DataTemplate x:DataType="Shell:ISettingViewModel">
                            <ListView
                                AllowDrop="{x:Bind IsReordering, Mode=OneWay}"
                                CanDragItems="{x:Bind SupportsReordering, Mode=OneWay}"
                                CanReorderItems="{x:Bind IsReordering, Mode=OneWay}"
                                DragItemsStarting="HandleModuleDragItemsStarting"
                                Margin="36,0,36,36"
                                ItemsSource="{x:Bind}"
                                ReorderMode="Enabled"
                                SelectionMode="None">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,0,0,4" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection>
                                        <EntranceThemeTransition />
                                        <RepositionThemeTransition />
                                    </TransitionCollection>
                                </ListView.ItemContainerTransitions>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl
                                            HorizontalContentAlignment="Stretch"
                                            Content="{Binding}"
                                            ContentTemplateSelector="{WinUI:ViewTemplateSelector}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </Grid>
        </NavigationView>
    </Grid>
</Window>

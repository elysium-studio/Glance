<Window
    x:Class="Glance.Shell.WinUI.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Shell="using:Glance.Shell"
    xmlns:WinUI="using:Elysium.UI.WinUI">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid Loaded="HandleLoaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar
            x:Name="AppTitleBar"
            x:Uid="SettingsTitleBar"
            BackRequested="HandleBackRequested"
            IsBackButtonEnabled="False"
            IsBackButtonVisible="False">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/GlanceLogo.png" />
            </TitleBar.IconSource>
        </TitleBar>
        <NavigationView
            x:Name="SettingsNavigation"
            Grid.Row="1"
            Margin="0,8,0,0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            PaneDisplayMode="Left"
            SelectionChanged="HandleNavigationSelectionChanged">
            <NavigationView.PaneFooter>
                <StackPanel Spacing="4">
                    <NavigationViewItem
                        x:Name="AboutNavigationItem"
                        x:Uid="AboutNavigationItem"
                        Margin="8,0,0,0"
                        SelectsOnInvoked="False"
                        Tapped="{x:Bind HandleAboutTapped}">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE946;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem
                        x:Name="QuitGlanceNavigationItem"
                        x:Uid="QuitGlanceNavigationItem"
                        Margin="8,0,0,0"
                        SelectsOnInvoked="False"
                        Tapped="{x:Bind HandleQuitTapped}">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE7E8;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </StackPanel>
            </NavigationView.PaneFooter>
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            </NavigationView.Resources>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <BreadcrumbBar
                    Grid.Row="0"
                    Margin="36,0,36,8"
                    ItemClicked="HandleBreadcrumbItemClicked"
                    ItemsSource="{x:Bind BreadcrumbItems}">
                    <BreadcrumbBar.Resources>
                        <ResourceDictionary>
                            <x:Double x:Key="BreadcrumbBarItemThemeFontSize">20</x:Double>
                            <Thickness x:Key="BreadcrumbBarChevronPadding">8,4,8,0</Thickness>
                            <FontWeight x:Key="BreadcrumbBarItemFontWeight">SemiBold</FontWeight>
                            <x:Double x:Key="BreadcrumbBarChevronFontSize">16</x:Double>
                        </ResourceDictionary>
                    </BreadcrumbBar.Resources>
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontFamily="Segoe UI Variable Display" Text="{Binding}" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
                <ContentControl
                    Grid.Row="1"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Content="{x:Bind ViewModel.CurrentView, Mode=OneWay}">
                    <ContentControl.ContentTransitions>
                        <TransitionCollection>
                            <ContentThemeTransition />
                        </TransitionCollection>
                    </ContentControl.ContentTransitions>
                    <ContentControl.ContentTemplate>
                        <DataTemplate x:DataType="Shell:ISettingViewModel">
                            <ListView
                                Margin="36,0,36,36"
                                ItemsSource="{x:Bind}"
                                SelectionMode="None">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,0,0,4" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl
                                            HorizontalContentAlignment="Stretch"
                                            Content="{Binding}"
                                            ContentTemplateSelector="{WinUI:ViewTemplateSelector}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </Grid>
        </NavigationView>
    </Grid>
</Window>

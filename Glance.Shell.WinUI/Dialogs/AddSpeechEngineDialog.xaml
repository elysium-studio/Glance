<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Glance.Shell.WinUI.AddSpeechEngineDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Shell="using:Glance.Shell"
    x:Uid="AddSpeechEngineDialog"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="{x:Bind ViewModel.CanAddProvider, Mode=OneWay}"
    PrimaryButtonClick="HandlePrimaryButtonClick"
    Style="{StaticResource DefaultContentDialogStyle}">
    <ListView
        Width="480"
        Height="240"
        ItemsSource="{x:Bind ViewModel.AvailableProviders, Mode=OneWay}"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        SelectedItem="{x:Bind ViewModel.SelectedProvider, Mode=TwoWay}"
        SelectionMode="Single">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="Shell:AssistantTranscriptionProviderViewModel">
                <StackPanel Padding="8" Spacing="2">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind DisplayName}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Description}" TextWrapping="Wrap" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentDialog>

# Glance

Glance is a lightweight WinUI desktop companion built around Elysium's `DesktopIsland` control.

The initial application intentionally contains only the shell required to open and demonstrate the island.

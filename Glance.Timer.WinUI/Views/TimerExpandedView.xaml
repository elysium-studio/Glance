<UserControl
    x:Class="Glance.Timer.WinUI.TimerExpandedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary Source="../Themes/Timer.xaml" />
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" Padding="20,12,20,26">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid
            x:Name="StatusIndicator"
            Width="36"
            Height="36"
            VerticalAlignment="Center">
            <FontIcon
                Width="30"
                Height="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="Segoe Fluent Icons"
                FontSize="30"
                Foreground="{ThemeResource GlanceTimerIconBrush}"
                Glyph="&#xE917;" />
        </Grid>
        <StackPanel
            Grid.Column="1"
            Margin="12,0,12,0"
            VerticalAlignment="Center"
            Spacing="2">
            <TextBlock
                FontSize="11"
                FontWeight="SemiBold"
                Foreground="{ThemeResource GlanceTimerIconBrush}"
                Text="{x:Bind ToUpper(ViewModel.Title)}" />
            <TextBlock
                FontSize="27"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                Text="{x:Bind ViewModel.RemainingText, Mode=OneWay}" />
        </StackPanel>
        <StackPanel
            Grid.Column="2"
            VerticalAlignment="Center"
            Orientation="Horizontal"
            Spacing="5">
            <Button
                Width="32"
                Height="32"
                Padding="0"
                AutomationProperties.Name="Decrease by one minute"
                Click="{x:Bind ViewModel.DecreaseMinute}"
                Content="&#xE738;"
                CornerRadius="16"
                FontFamily="Segoe Fluent Icons"
                IsEnabled="{x:Bind ViewModel.CanDecreaseMinute, Mode=OneWay}"
                ToolTipService.ToolTip="Decrease by one minute" />
            <Button
                Width="32"
                Height="32"
                Padding="0"
                AutomationProperties.Name="Increase by one minute"
                Click="{x:Bind ViewModel.AddMinute}"
                Content="&#xE710;"
                CornerRadius="16"
                FontFamily="Segoe Fluent Icons"
                ToolTipService.ToolTip="Increase by one minute" />
            <Button
                Width="32"
                Height="32"
                Padding="0"
                Click="{x:Bind ViewModel.Toggle}"
                Content="{x:Bind ViewModel.ToggleGlyph, Mode=OneWay}"
                CornerRadius="16"
                FontFamily="Segoe Fluent Icons"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                Width="32"
                Height="32"
                Padding="0"
                Click="{x:Bind ViewModel.Reset}"
                Content="&#xE777;"
                CornerRadius="16"
                FontFamily="Segoe Fluent Icons"
                ToolTipService.ToolTip="Reset" />
        </StackPanel>
    </Grid>
</UserControl>

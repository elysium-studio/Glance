<UserControl
    x:Class="Glance.Reminders.WinUI.ReminderExpandedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="using:Elysium.UI.Controls.WinUI"
    xmlns:Reminders="using:Glance.Reminders">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Reminders.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate x:Key="ReminderItemTemplate" x:DataType="Reminders:ReminderItemViewModel">
                <Grid Margin="2,0,0,0" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Spacing="1">
                        <Controls:MarqueeTextBlock
                            Height="20"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            Text="{x:Bind Title}"
                            ToolTipService.ToolTip="{x:Bind Title}" />
                        <Controls:MarqueeTextBlock
                            Height="16"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind DetailText}"
                            ToolTipService.ToolTip="{x:Bind DetailText}" />
                    </StackPanel>
                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="2">
                        <Button Command="{x:Bind EditCommand}" ToolTipService.ToolTip="Edit reminder">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SubtleButtonStyle}" TargetType="Button">
                                    <Setter Property="Width" Value="32" />
                                    <Setter Property="Height" Value="32" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Content" Value="&#xE70F;" />
                                    <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
                                    <Setter Property="FontSize" Value="14" />
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{x:Bind RemoveCommand}" ToolTipService.ToolTip="Delete reminder">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SubtleButtonStyle}" TargetType="Button">
                                    <Setter Property="Width" Value="32" />
                                    <Setter Property="Height" Value="32" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Content" Value="&#xE74D;" />
                                    <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
                                    <Setter Property="FontSize" Value="14" />
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Padding="20,12,20,26" ColumnSpacing="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid
            x:Name="StatusIndicator"
            Width="36"
            Height="36"
            VerticalAlignment="Center">
            <FontIcon
                Width="30"
                Height="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="Segoe Fluent Icons"
                FontSize="30"
                Foreground="{ThemeResource GlanceRemindersIconBrush}"
                Glyph="&#xE823;" />
        </Grid>
        <Grid
            Grid.Column="1"
            Height="64"
            VerticalAlignment="Center"
            RowSpacing="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="14" />
                <RowDefinition Height="22" />
                <RowDefinition Height="18" />
            </Grid.RowDefinitions>
            <TextBlock
                FontWeight="SemiBold"
                Foreground="{ThemeResource GlanceRemindersIconBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ToUpper(ViewModel.Title)}" />
            <TextBlock
                Grid.Row="1"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind EmptySummary}"
                Visibility="{x:Bind WhenEmpty(ViewModel.HasReminders), Mode=OneWay}" />
            <TextBlock
                Grid.Row="2"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind EmptyDetail}"
                Visibility="{x:Bind WhenEmpty(ViewModel.HasReminders), Mode=OneWay}" />
            <Controls:CompactFlipView
                x:Name="ReminderFlipView"
                Grid.Row="1"
                Grid.RowSpan="2"
                Margin="-22,0,0,0"
                Background="Transparent"
                ItemTemplate="{StaticResource ReminderItemTemplate}"
                ItemsSource="{x:Bind ViewModel.Reminders}"
                SelectedItem="{x:Bind ViewModel.SelectedReminder, Mode=TwoWay}"
                Visibility="{x:Bind WhenPopulated(ViewModel.HasReminders), Mode=OneWay}" />
        </Grid>
        <Button
            Grid.Column="2"
            Width="36"
            Height="36"
            VerticalAlignment="Center"
            Command="{x:Bind ViewModel.AddCommand}"
            ToolTipService.ToolTip="{x:Bind AddLabel}">
            <Button.Style>
                <Style BasedOn="{StaticResource AccentButtonStyle}" TargetType="Button">
                    <Setter Property="Width" Value="36" />
                    <Setter Property="Height" Value="36" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="CornerRadius" Value="18" />
                    <Setter Property="Content" Value="&#xE710;" />
                    <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
                    <Setter Property="FontSize" Value="14" />
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
